- breadcrumb :payments
%table.table.table-condensed.datatables
  %thead
    %tr
      %th= t('student.columns.first_name')
      %th= t('student.columns.last_name')
      %th= t('payment.columns.last_payment')
      %th= t('payment.columns.current_payment')
      %th= t('shared.actions')
  %tbody
    = render partial: 'student', collection: students
