- breadcrumb :payments
%table.table.table-condensed
  %thead
    %tr
      %th= t('student.columns.first_name')
      %th= t('student.columns.last_name')
      %th= t('payment.columns.deadline')
      %th= t('payment.columns.paid')
      %th= t('shared.actions')
  %tbody
    = render payments
